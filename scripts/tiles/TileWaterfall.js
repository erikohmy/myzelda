class TileWaterfall extends TileAnimated4 {
    spriteOffsetY = 8;
    solid = true;
    // middle, bottom
    // waterfall start (waterfall two bottom tiles, black animated top two) (solid)
    // waterfall start (cave) black animated bottom 2, and black rest
}